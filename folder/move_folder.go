package folder

// TODO: errors, error messages 
func (f *driver) MoveFolder(name string, dst string) ([]Folder, error) {
	// (TODO: function description)

	// basic functionality: every path that has string, add dst before it
	// (todo: other cases)
	// iterate through every folder path
	// if it "Contains" (string function - import) a "name.", then add "string." prior to it
	// (checking needs to be done before it)

	return []Folder{}, nil
}

// TODO: helper checking functions
